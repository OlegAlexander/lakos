name: lakos
description: Visualize imports as a dot graph.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.7.0 <3.0.0"

executables:
  lakos:

dependencies:
  path: ^1.6.0
  gviz: ^0.3.0

dev_dependencies:
  pedantic: ^1.8.0
  test: ^1.14.2
